{
  "environments": {
    "test": {
      "scripts": {
        "test-setup": "ts-node src/seeds/seed",
        "test": "jest --maxWorkers=1"
      },
      "addons": ["heroku-postgresql:in-dyno"]
    }
  }
}